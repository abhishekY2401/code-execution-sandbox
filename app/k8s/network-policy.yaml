apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-job-communication
spec:
  podSelector:
    matchLabels:
      job-name: code-runner
  policyTypes:
    - Egress
  egress: []